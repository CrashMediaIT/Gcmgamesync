services:
  gcmgamesync:
    build: .
    ports:
      - "8080:8080"
    environment:
      GCM_DATA_DIR: /data
      GCM_ADMIN_EMAIL: admin@example.com
      GCM_ADMIN_PASSWORD: change-this-admin-password
    volumes:
      - gcmgamesync-data:/data
volumes:
  gcmgamesync-data:
